 Основной сценарий (поток обработки)
Загружаем данные из Google Sheets.

Оставляем только строки, где статус != "Готов".

Обрабатываем только новые строки — проверка по index в JSON.

Вызываем DataCleaner:

clean_vehicle_names() — очистка номера машины.

add_id_to_data() — добавление ID из Id_car.json.

start_clean() — разбор строк "Погрузка" и "Выгрузка".

Запускаем WebDriver (две вкладки: Wialon и Яндекс.Карты).

Для каждой машины:

Ввод ТС → поиск в Wialon → получение адреса и координат → запись в JSON.

Переключение на Яндекс.Карты → ввод маршрута → получение всех маршрутов → расчёт прибытия → запись в JSON.

Выводим в таблицу данные из JSON.

🟦 Wialon (NavigationBot)
Логин через login_wialon().

Ввод номера ТС в поиск.

Проверка по car_id.

Получение адреса по addressName_WTb9.

Копирование координат через кнопку .icon-copy-coordinates.

Очистка строки поиска (clean_car).

Запись координат и адреса в JSON.

В лог GUI отправляются этапы: поиск, гео, ошибки.

🟨 Яндекс.Карты (MapsBot)
open_yandex_maps() — открытие и переключение вкладки.

click_route_button() — нажать "Маршруты".

enter_route():

Вставка коор в "Откуда"

Вставка Выгрузка 1 в "Куда"

Несколько нажатий Enter, если адрес не срабатывает.

get_route_info():

Получение списка маршрутов.

Если маршруты не появились, fallback на get_first_route().

calculate_arrival_time_from_minutes():

Расчёт времени прибытия (текущая дата + среднее время).

Вычисление буфера.

Все этапы логируются.

🟧 Google Sheets
Загружается весь лист, отбираются только != "Готов".

Только новые index добавляются в JSON.

Старые, у которых статус стал "Готов", удаляются.

Запись гео + координат в отдельную колонку Google Sheets по index.

🟥 Интерфейс (NavigationGUI)
Кнопка "▶" обрабатывает только одну строку.

Используется ThreadPoolExecutor для асинхронной работы.

Лог отображается в нижнем окне.

Двойной клик по ячейке открывает окно редактирования AddressEditDialog.

Столбцы:

id, ТС — фиксированная ширина.

Погрузка, Выгрузка — по 500px.

Время погрузки/выгрузки — не редактируемые.

Погрузки/выгрузки нумеруются (если >1).

btn_process_all() — обрабатывает весь список по очереди.

📌 Технические улучшения
Добавлен log_func для отправки логов в интерфейс вместо print.

Все боты поддерживают driver.switch_to.window(...) для переключения вкладок.

Проверка и задержки после ввода (в т.ч. ожидание маршрутов).

Возможность редактирования Погрузка/Выгрузка через AddressEditDialog.

🗂️ TO DO / Планы
Вынести web_driver_wait и log() в BaseBot.

Сделать README.md с описанием проекта и запуском.

Добавить полноценные docstrings к методам.

Сделать фильтр в GUI по времени прибытия.

Возможность в будущем обрабатывать Погрузка 2, Выгрузка 2 и т.д.